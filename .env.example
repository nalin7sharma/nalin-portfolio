# Optional contact endpoint override.
# By default, the cPanel build posts to /api/contact.php, which is included in public/api.
# Use this only if you replace the PHP endpoint with Formspree, EmailJS proxy, or another provider.
NEXT_PUBLIC_CONTACT_ENDPOINT=
